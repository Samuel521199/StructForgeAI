# StructForge AI - 完整安装和启动指南

## 📋 完整脚本体系

项目已配置完整的批处理脚本体系，便于统一管理：

### 后端脚本
- `backend/setup_conda.bat` - 创建Conda环境（首次安装）
- `backend/start_conda.bat` - 启动后端服务
- `backend/install_deps.bat` - 安装/更新依赖
- `backend/verify_install.bat` - 验证安装

### 前端脚本
- `frontend/setup_frontend.bat` - 安装前端依赖（首次安装）
- `frontend/start_frontend.bat` - 启动前端服务

### 统一管理脚本
- `start_all.bat` - 一键启动所有服务（推荐）
- `stop_all.bat` - 停止所有服务

## 🚀 快速开始

### 首次安装（只需一次）

#### 1. 安装后端环境

```powershell
cd F:\StructForgeAI\backend
.\setup_conda.bat
```

#### 2. 安装前端依赖

```powershell
cd F:\StructForgeAI\frontend
.\setup_frontend.bat
```

### 日常启动（每次使用）

#### 方式1：一键启动所有服务（推荐 ⭐）

```powershell
# 在项目根目录
cd F:\StructForgeAI
.\start_all.bat
```

然后选择选项：
- `3` - 启动后端和前端（推荐）

#### 方式2：分别启动

**启动后端：**
```powershell
cd F:\StructForgeAI\backend
.\start_conda.bat
```

**启动前端（新终端窗口）：**
```powershell
cd F:\StructForgeAI\frontend
.\start_frontend.bat
```

### 停止服务

#### 方式1：使用脚本

```powershell
.\stop_all.bat
```

#### 方式2：手动关闭

- 直接关闭对应的命令行窗口
- 或按 `Ctrl+C` 停止服务

## 📁 脚本说明

### start_all.bat - 统一启动脚本

**功能**：
- 检查环境是否就绪
- 提供启动选项（后端/前端/全部）
- 在新窗口中启动服务，便于管理

**使用方法**：
```powershell
.\start_all.bat
```

**选项**：
1. 仅启动后端
2. 仅启动前端  
3. 启动后端和前端（推荐）
4. 取消

### stop_all.bat - 统一停止脚本

**功能**：
- 自动查找并停止所有服务进程
- 停止Python后端进程
- 停止Node.js前端进程

**使用方法**：
```powershell
.\stop_all.bat
```

## 🎯 访问地址

启动成功后访问：

- **前端界面**：http://localhost:3000
- **后端API**：http://localhost:8000
- **API文档**：http://localhost:8000/docs
- **健康检查**：http://localhost:8000/health

## 📝 使用流程

### 开发模式

1. **启动服务**：
   ```powershell
   .\start_all.bat
   # 选择选项 3
   ```

2. **开发**：
   - 修改后端代码（自动重载）
   - 修改前端代码（自动重载）

3. **停止服务**：
   ```powershell
   .\stop_all.bat
   ```
   或直接关闭命令行窗口

### 生产部署

1. **启动后端**（后台运行）：
   ```powershell
   cd backend
   start_conda.bat
   ```

2. **启动前端**（构建生产版本）：
   ```powershell
   cd frontend
   npm run build
   npm run preview
   ```

## 🔧 故障排查

### 后端无法启动

1. 检查conda环境：
   ```powershell
   conda env list
   ```

2. 检查依赖：
   ```powershell
   cd backend
   .\verify_install.bat
   ```

### 前端无法启动

1. 检查Node.js：
   ```powershell
   node --version
   npm --version
   ```

2. 重新安装依赖：
   ```powershell
   cd frontend
   .\setup_frontend.bat
   ```

### 端口被占用

**后端端口（8000）被占用**：
- 修改 `backend/main.py` 中的端口号
- 或使用 `uvicorn main:app --port 8001`

**前端端口（3000）被占用**：
- 修改 `frontend/vite.config.ts` 中的端口号

## 📚 相关文档

- [Conda安装指南](CONDA_SETUP_GUIDE.md) - Conda环境详细说明
- [通用安装指南](SETUP_GUIDE.md) - venv安装方法
- [快速启动指南](QUICK_START.md) - 快速上手指南

---

**使用 `start_all.bat` 一键启动，方便快捷！** 🎉

